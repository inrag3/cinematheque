<%= render "shared/search" %>
<div class="container">
  <div class="movies">
    <% if @movies.any? %>
      <%= render 'movies/movie' %>
    <% end %>
  </div>
  <%= render partial: "shared/pagination", locals: {path: browse_path, search: nil} %>
</div>

